{
  "name": "mx",
  "version": "1.0.0",
  "description": "Monorepo CLI to scaffold & run BE (Bun) + FE (Next.js) apps",
  "type": "module",
  "bin": {
    "mx": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
