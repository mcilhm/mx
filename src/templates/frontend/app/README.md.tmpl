# fe-__APP_NAME__ (Frontend)

Next.js (App Router) frontend app, scaffolded into `frontend/apps/__APP_NAME__/`.

## Scripts

```bash
bun install
bun run dev      # http://localhost:__PORT__
bun run build
bun run start
```

Default port: `__PORT__`.
