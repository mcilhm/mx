# be-__APP_NAME__ (Backend)

Bun + Elysia backend app, scaffolded into `backend/apps/__APP_NAME__/`.

## Scripts

```bash
bun install
bun run dev      # hot reload
bun run build    # bundle to dist/
bun run start    # run the bundle
```

Default port: `3001` (override with `PORT=` or `.env`).
